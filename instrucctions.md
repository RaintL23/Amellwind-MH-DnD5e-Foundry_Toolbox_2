# Domain docs moved

The technical domain documentation formerly in this file now lives under **[`docs/domain/`](./docs/domain/index.md)**, split into focused files so agents and humans can open only what they need.

1. Start at [`docs/domain/index.md`](./docs/domain/index.md).
2. Open **one** matching file (or `grep` a heading inside it).
3. Do **not** read the whole `docs/domain/` folder.

Operational agent guidance (commands, conventions, complex zones): [`AGENTS.md`](./AGENTS.md).
