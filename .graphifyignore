# Only index application source. Root docs can be added explicitly:
#   graphify extract ./src ./AGENTS.md ./docs/domain ./README.md
# .gitignore is also respected (public/5etools, backup_jsons, dist, etc.).

*
!src/
!src/**

# Project docs (opt-in when not using --code-only)
!AGENTS.md
!instrucctions.md
!README.md
!docs/
!docs/**
!docs/domain/
!docs/domain/**
