# Equipment, Resources, and Crafting

This directory is reserved for curated GTMH Patreon extracts:

- `weapons.md` for hunter weapon tables (`item[]` with `type: "HW"`)
- `items.md` for new items and magic items (`item[]`)
- `optional-features.md` for weapon optional features (`optionalfeature[]`)
- `siege-weapons.md` for AGMH objects (`object[]`, `objectType: "SW"`)

Current runtime behavior in this PR:
- merge pipeline is enabled for these keys
- GitHub keeps filling any key with an empty local array
