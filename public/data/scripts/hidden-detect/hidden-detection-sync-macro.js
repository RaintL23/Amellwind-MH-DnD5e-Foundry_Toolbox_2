// Hidden Detection Sync — Amellwind (Foundry v12 / dnd5e 4.4)
// Run once as GM after placing hidden objects (or on world load via Advanced Macros).
// Prefer: open Configure on a Hidden Detection feature — that also arms hooks.

/* @@SYNC_ENGINE@@ */

await syncHiddenDetection({ notify: true });
